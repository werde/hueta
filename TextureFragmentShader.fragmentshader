#version 120 core

uniform sampler2D myTextureSampler;

uniform vec3 ex_Color;

varying vec2 UV;

void main()
{
	vec2 texCoords = vec2(UV);
	vec4 color = texture2D(myTextureSampler, UV);
	gl_FragColor = color;
}